struct gamepad_lcmt {
    int32_t leftBumper, rightBumper, up,down,left,right, back, start, a,b,x,y,leftStickButton, rightStickButton;
    float leftTriggerAnalog, rightTriggerAnalog;
    float leftStickAnalog[2];
    float rightStickAnalog[2];
}
